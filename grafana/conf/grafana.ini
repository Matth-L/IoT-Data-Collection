[smtp]
enabled = true
host = ${SMTP_HOST}:${SMTP_PORT}
proto = tls
from_address = ${SMTP_USER}
from_name = Grafana
ehlo_identity =
user =  ${SMTP_USER_ID}
password =  ${SMTP_USER_PW}

cert_file =
key_file =
skip_verify = false
